# Небольшая программа на C++, которая **выдаёт массив целых чисел в обратном порядке** и выводит результат в консоль.

## Что делает код
- Создаёт массив
- Меняет элементы местами “с краёв к центру” (через два указателя: `left` и `right`)
- Выводит получившийся массив
